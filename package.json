{
  "type": "module",
  "scripts": {
    "test": "NODE_ENV=test mocha"
  },
  "mocha": {
    "spec": "api/tests/**/*.test.js",
    "timeout": "2000",
    "exit": true,
    "watch-files": [
      "api/tests/**/*.test.js"
    ]
  },
  "dependencies": {
    "express-ensure-ctype": "^1.0.0",
    "json-merge-patch": "^1.0.2",
    "json-patch": "^0.7.0"
  }
}
